const numbers = [1, 2, 13, 4, 5, 8];

const sum = 0;
numbers.forEach(num => {
    sum += num;
})
console.log



const myFriends = ["Rupesh", "John", "vishal", "Virat"];

myFriends.forEach(function (element, index, array) {
    console.log(element);
    console.log(index);
    console.log(array);
    console.log([element, index, array]);
    console.log(element + " index : " + index);
})
